# BananaPad journal

Append-only engineering record. Evidence remains local and ignored where it may contain private or ROM-derived material.

## 2026-08-31 11:17 CDT — G4 accepted; macOS-first core and Simulator regression green

- **Lowest unmet goal:** G4.
- **Hypothesis:** the promoted Apple static profile can satisfy native macOS video, audio, input, static-patch/overlay, diagnostics, and clean-exit requirements before the same source is regression-tested in iPadOS Simulator.
- **Step:** launch and observe the exact hardened macOS app; capture Metal output and runtime/audio diagnostics; deliver native Start input; diagnose a termination hang; change only the Apple no-dynamic-code shutdown boundary so host-owned threads finish without joining the parked guest thread; rebuild and audit; repeat clean-exit testing; refresh the external patch; stage a clean candidate; build, install, launch, visibly inspect, smoke, and audit it in the one isolated iPad Pro Simulator; rehearse promotion, apply, roll back, reapply, and archive the candidate recoverably.
- **Result:** successful. macOS rendered DK64, produced active CoreAudio output, accepted input, and exited twice within 250 ms. The exact Simulator executable `47cf2d83e266422ae5980de8b1daabb7cdff9dd4f8291479448886078c18d69a` visibly rendered DK64 with touch controls and the three-dot menu and passed the ROM-bound 20-second smoke plus static-code/package/execution-model/update audits. Promotion/rollback/re-promotion reproduced lock SHA-256 `6be868adab31ee27b27923b72358680b7608ea32a1a5c54bf0aabaa4d799104b`.
- **Evidence:** `docs/VALIDATION-G4-2026-08-31.md`, ignored macOS/Simulator screenshots and receipt, promotion snapshots `20260831T161623Z` and `20260831T161638Z`, and recoverable candidate archive `20260831T161701Z`.
- **State after action:** no BananaPad process and no booted Simulator; the verified private ROM remains outside tracked paths.
- **Interpretation:** G4 is met. Physical iPad hardware was never a G4 dependency and remains a later G12 acceptance gate.
- **Next lowest goal:** G5.

## 2026-08-31 00:05 CDT — G0 initial state and reference acquisition

- **Lowest unmet goal:** G0 — Environment, references, and safety ready.
- **Hypothesis:** the local host can establish the exact pinned reference and safety boundary without modifying the user's ROM or existing uncommitted documentation move.
- **Pre-action state:** root `89a92d040e73917335fb2c2d648afee33ab39369`; `GOAL-LOOP.md` and `PRD.md` were already deleted at root and present untracked under `docs/`; no status, journal, rights record, dependency lock, or safety scripts existed; no Simulator or game instance was running.
- **Step:** read `docs/GOAL-LOOP.md` and `docs/PRD.md`; inspect the PaperPad README/architecture and the prescribed DK64/SunPad reference documents; add the ignore boundary; download the missing DK64Recompiled and SunPad references recursively; pin all three references; disable their push URLs; inspect the host toolchain and current upstream tag/main identities.
- **Commands:** read-only `git status`, `git rev-parse`, `xcrun simctl`, `ps`, `file`, `stat`, `shasum`, tool version checks, `git ls-remote`; `git clone --recursive`; detached checkouts to the PRD pins; recursive submodule initialization; `git remote set-url --push origin DISABLED`.
- **Result:** successful. References are local at the expected pins; DK64 recursive submodule identity hashes to `bebfa8f085ae40eafed4ca3d79fbbd7bf5566114a550b11878fba181c7ffc088`; no newer stable tag exists at this observation.
- **Private input identity:** original filename `Donkey Kong 64 (U) [!].v64`; byte order V64; length 33,554,432; SHA-1 `3ead7798284c0593d8c91e09923cde03e87c6b09`; SHA-256 `5778c9ef72ef269cdcc52333710a79961a343b1f01d12189d1dbe94df3cbabed`. The original was not modified.
- **Evidence:** this journal entry, `dependencies.lock.json`, and local reference Git metadata. No gameplay/build claim is made.
- **Interpretation:** reference acquisition and the basic host toolchain are viable. G0 remains open until executable checks pass and all required inventories/read-only guarantees are in place.
- **Next step:** run the new G0 checks and repair any concrete failure before proceeding to G1.

## 2026-08-31 00:12 CDT — G0 released host-tool pin

- **Lowest unmet goal:** G0.
- **Hypothesis:** DK64's released N64Recomp/RSPRecomp generator revision builds natively on this Apple Silicon host from pinned public source.
- **Step:** clone `Mr-Wiseguy/N64Recomp` recursively at `2b6f05688de2abc7d86da5b4a89b84c2c6acbabe`, disable push, configure Release with AppleClang and Ninja, and build `N64Recomp` plus `RSPRecomp` under ignored `generated/build/host-tools`.
- **Result:** successful. `N64Recomp` SHA-256 `1ce3c89269e2c609bdd056d11f03b8470b30eeae3d09c217193a2e71fbd4ecdf`; `RSPRecomp` SHA-256 `637e517911d9461d4cb45d9e59de11102a10629ad83fb548f2465c49dfa1b792`. The linker emitted one duplicate-static-library warning; both executables linked. These revisions treat their first argument as a TOML path and do not implement `--help`, so the observed nonzero `--help` result is not a build failure.
- **Evidence:** build transcript in the task terminal, hashes above, source lock, and host-tool recursive manifest `27357f050f3dc6be8470d1471c2487557941d4a0161404019788a46e9d62ad6b`.
- **Interpretation:** the released generator toolchain is available for G1; no generated game/RSP output has been claimed yet.
- **Next step:** finish the reference/source map and G0 automated checks.

## 2026-08-31 00:18 CDT — G0 accepted

- **Lowest unmet goal:** G0.
- **Hypothesis:** the accumulated pin, toolchain, rights, and safety state satisfies G0 and can be rerun without relying on terminal archaeology.
- **Step:** rerun prerequisite, reference, repository-safety, upstream-discovery, script-syntax, JSON, and whitespace checks; smoke the no-dynamic-code audit against a benign system Mach-O; record dedicated validation evidence.
- **Result:** all checks passed. No Simulator or game instance was running. The existing user documentation relocation remains preserved and no remote push/publication occurred.
- **Evidence:** `docs/VALIDATION-G0-2026-08-31.md`, `dependencies.lock.json`, `docs/SOURCE-MAP.md`, and executable scripts under `scripts/`.
- **Interpretation:** G0 is met. This is readiness evidence only; no ROM-derived output or product behavior is claimed.
- **Next lowest goal:** G1.
- **Next step:** normalize the original V64 into an ignored Z64 copy with exact size/hash verification, then prove deterministic decompression before game/patch/RSP generation.

## 2026-08-31 00:31 CDT — G1 accepted

- **Lowest unmet goal:** G1.
- **Hypothesis:** the supplied V64 and pinned public inputs can deterministically reproduce the standard ROM identity, appended decompressed layout, game code, patch code/data, and audio RSP without modifying references or the original.
- **Step:** implement atomic three-byte-order normalization; verify exact SHA-1/header; compile the pinned decompressor outside the reference; decompress twice; build the released N64Recomp/RSPRecomp; generate game/RSP twice; install LLVM/LLD 18 for MIPS; build/generate patches twice; compare manifests; derive the overlay layout from the verified source/input; rerun safety and pin checks.
- **Result:** successful. All hashes and warnings are captured in `VALIDATION-G1-2026-08-31.md`; the original hashes remained unchanged; every ROM-derived/generated output is ignored.
- **Evidence:** `docs/VALIDATION-G1-2026-08-31.md`, `docs/ROM-INPUTS.md`, content-addressed ignored manifests under `generated/aot`, and the executable pipeline scripts.
- **Interpretation:** G1 is met. The ambiguous shared-overlay call warning and upstream patch compiler warnings remain explicit G3 audit inputs; they do not invalidate deterministic generation.
- **Next lowest goal:** G2.
- **Next step:** reproduce the pristine `1.0.1` macOS app from these inputs, inspect the exact artifact, then run/import/play/save/reload hands-on and rehearse same-pin upstream staging/rollback.

## 2026-08-31 01:27 CDT — G2 build, launch, ROM acceptance, and sync rehearsal

- **Lowest unmet goal:** G2.
- **Hypothesis:** pinned `1.0.1` can be reproduced locally from G1 products, and its future-update mechanics can be proven without mutating the promoted lock or pristine references.
- **Step:** create a detached ignored baseline worktree; configure/build the arm64 macOS app; retain the first Xcode 26 compile failure; add a one-line external `labs` declaration patch; retain the first SDL3 launch failure; complete the private bundle with SDL3; deep-verify and audit the bundle; launch before and after installing the exact app-owned ROM copy; stage/test/rehearse/rollback the same upstream pin.
- **Result:** partial success. The final bundle is deeply signature-valid, launches, changes from `Load ROM` to `Start Game` for the exact ROM, and the synchronization rehearsal passes with an unchanged lock. The upstream bundle predictably fails the no-dynamic-code audit. The Metal canvas prevents the computer-control state handshake, so no blind input was sent and gameplay/save/reload remain unclaimed.
- **Command error retained:** an interactive zsh aggregate-hash check used the special `path` variable and temporarily removed `shasum` from command lookup, producing an empty hash. The evidence command was rerun under Bash and the correct bundle hash is recorded; the empty hash is discarded.
- **Evidence:** `docs/VALIDATION-G2-2026-08-31.md`, ignored screenshots, bundle/code hashes, exact compatibility patch, and ignored same-pin rollback archive.
- **State after action:** no running DK64 process and no booted Simulator. The ROM-derived app-owned copy is mode `0600` outside the repository; original and generated private inputs remain unchanged.
- **Interpretation:** source/build/launch/ROM-recognition/update mechanics are viable, but G2 is not met.
- **Next step:** obtain observable `Start Game` interaction, gameplay, save creation, clean termination, and reload evidence without weakening the one-process discipline.

## 2026-08-31 01:42 CDT — G2 recursive candidate-state hardening

- **Lowest unmet goal:** G2.
- **Hypothesis:** a candidate promotion must detect changes inside nested submodules after testing, not merely retain the same top-level commit.
- **Step:** add a deterministic recursive worktree digest covering tracked diffs and untracked paths at the superproject and every nested submodule; bind it into candidate metadata; recheck it during test and promotion; repeat the same-pin stage/test/promotion-rehearsal/rollback cycle.
- **Result:** successful. The final recursive worktree digest was `d68627960485cd09a1401bfe15a4ed7a608e690d66c8d1d5409377c4274db479`; the source manifest and patch series still matched their locks; test and promotion rehearsal passed; rollback moved the candidate recoverably to ignored `generated/upstream/rollbacks/20260831T064155Z`; no active candidate remains.
- **Evidence:** candidate/validation JSON in the ignored rollback archive, the synchronization scripts, and `docs/VALIDATION-G2-2026-08-31.md`.
- **Interpretation:** same-pin update mechanics now reject post-test drift throughout the recursive graph. This still does not establish compatibility with a future upstream change and does not close G2.
- **Next step:** obtain observable gameplay and save/reload evidence from the reproduced upstream comparison process.

## 2026-08-31 02:01 CDT — G2 upstream behavior contract archived

- **Lowest unmet goal:** G2.
- **Hypothesis:** source and artifact evidence can close the remaining comparison-contract gaps without misrepresenting the still-unobserved gameplay/save path.
- **Step:** retry Computer Use state through app path, bundle ID, and foreground activation; stop after repeated timeout; inspect the pinned launch, input, renderer, audio, and save implementations; add `docs/UPSTREAM-BASELINE.md`; add and run `scripts/audit-upstream-baseline.sh`; wire that audit into same-pin candidate testing.
- **Result:** the control bridge timed out in all supported state modes and refused actions without a successful state handshake. No blind or alternate OS event synthesis was used. The new audit passed against the promoted commit and exact reproduced app, verifying the source constants, deep signature, native architecture, bundle ID, expected desktop entitlements/RWX maximum protection, patch-set identity, and stored-ROM identity. The expected `saves/DK64.bin` remains absent.
- **Evidence:** `docs/UPSTREAM-BASELINE.md`, `docs/VALIDATION-G2-2026-08-31.md`, and executable baseline/update audits.
- **Interpretation:** the launcher/input/audio/save contract and known exceptions are now explicit and drift-detected. Gameplay, actual audio/input, save creation, and reload remain unproven, so G2 remains open.
- **Next step:** obtain a supported observable input route for the exact comparison process, then play, save, terminate, relaunch, and visibly reload progress.

## 2026-08-31 03:28 CDT — G2 hands-on evidence route prepared; goal blocked

- **Lowest unmet goal:** G2.
- **Hypothesis:** if automated native control remains unavailable, the remaining human observation can still be bounded, identity-checked, private, recoverable, and resistant to false save evidence.
- **Step:** add `scripts/validate-upstream-play-session.sh` and `docs/G2-HANDS-ON.md`; require exact baseline/ROM preflight, a changed nonzero 2048-byte save, clean process boundaries, private backup, exact-app relaunch, reload PNG, and written observation; validate syntax/help/inactive status and privacy behavior without launching or fabricating a session.
- **Result:** harness checks pass; script SHA-256 is `076cbf12c7e831e4030bf4b44cac5ca2fec01e1a5d1630ddaf6484bba11d4d33`. No play session, save, process, or Simulator exists. The same Computer Use state failure has now persisted across at least three consecutive goal turns and prevents any policy-compliant automated input.
- **Evidence:** `docs/G2-HANDS-ON.md`, the executable session harness, the passing upstream baseline audit, and absence checks.
- **Interpretation:** all safe autonomous G2 work available without misrepresenting gameplay is exhausted. The goal is blocked pending a hands-on evidence session or explicit authorization for alternate macOS CGEvent/AppleScript input synthesis. G2 is not met and G3 must not start.
- **Unblock command:** `scripts/validate-upstream-play-session.sh begin`, followed by the documented play/save/reload route; alternatively, Chris may explicitly authorize alternate macOS input synthesis.

## 2026-08-31 05:28 CDT — macOS execution restored; static native-shell ladder active

- **Lowest unmet goal:** G2 remains open for save/reload evidence; G3/G4 macOS architecture work is active under the user's explicit macOS-first direction.
- **Hypothesis:** the prior Computer Use accessibility timeout is not a blocker to building, launching, observing, and debugging an SDL/Metal app on this Mac; window-specific screen capture and attached runtime logs can establish actual execution while the Apple shell is hardened.
- **Step:** download PaperPad's pinned nested sources; build the exact pinned SDL2 static library; add a BananaPad arm64 bundle identity and deterministic isolated macOS build; install the exact private ROM outside the repository; add a compile-time `N64MODERN_NO_DYNAMIC_CODE` profile; remove the writable-text link route and forbidden entitlements; repair embedded-library signing; replace Homebrew sdl2-compat/SDL3 with PaperPad SDL2 static; bypass the desktop launcher and direct-start the registered DK64 game; copy PaperPad's Apple shell sources byte-for-byte, including its touch overlay and three-dot menu; build, audit, launch, and capture the exact artifact repeatedly.
- **Result:** successful macOS execution. `generated/build/bananapad-static-macos/BananaPad.app` starts DK64 without an environment flag, reaches the DK Rap intro through Metal, and remains live during bounded observation. Its executable SHA-256 is `64a40a77bd75c12d4f471da73631a7e9a2f286ec79448cc99d54cc4014cf8b9a`. The no-dynamic-code audit, deep signature verification, forbidden-entitlement check, and W+X Mach-O check pass. SDL2 is statically linked; the prior SDL2→SDL3 dylib chain is absent. No game process remains after the test.
- **Defect found and fixed:** the first hardened bundle enabled Hardened Runtime with an ad-hoc signature while embedding separately signed Homebrew dylibs, causing dyld to reject the SDL library for a Team-ID mismatch. The build now signs the local bundle consistently without forbidden entitlements, and the pinned static SDL2 path removes that compatibility library entirely.
- **Evidence:** `generated/evidence/bananapad-macos/game-running-clean.png`, `generated/evidence/bananapad-static-macos/game-window.png`, `game-window-pinned-sdl2.png`, and `native-shell-direct-boot.png`; attached runtime log includes recomp heap initialization, ROM DMA, and `func_global_asm_805FBFF4 Started`.
- **Interpretation:** BananaPad was not blocked on iPad hardware or Simulator. macOS build/run/testing works on this machine. G4 is not yet accepted because desktop RmlUi/config code still links even though its launcher is bypassed; that dependency is the next macOS defect to remove before Simulator.
- **Next step:** adapt the vendored PaperPad static renderer/input/audio shell to DK64, keep the verbatim touch/menu source as the UI reference layer, prove the shared core again on macOS, then build/install/launch it in iPadOS Simulator.

## 2026-08-31 08:18 CDT — iPadOS Simulator DK64, touch UI, and repeatable launch accepted

- **Sequence:** macOS stabilization remained first. The direct Metal core was built and exercised on macOS through input, clean termination, and save/reload before configuring the iOS Simulator target. No physical iPad was used or required.
- **Hypothesis:** PaperPad's pinned Apple shell and its known RT64 Simulator compatibility changes can carry the now-stable BananaPad core into a local iPad Pro Simulator without replacing the renderer or inventing a second UI.
- **Step:** add the iOS bundle/resources; compile pinned SDL2 into the iOS target; use native host shader tools while cross-compiling; install the exact private ROM in the isolated Simulator app container; launch with attached console; symbolicate each crash; apply the PaperPad resource-limit guards; repair null Simulator timestamp results; regenerate stale included shaders; rebuild, sign, install, launch, capture, and operate the UI through accessibility-backed Computer Use; codify the successful flow in `scripts/build-bananapad-ios-simulator.sh`; run that script end to end.
- **Failures retained:** `-[MTLSimDevice location]` selector failure; 52-buffer debug-pipeline rejection; 18-sampler raster-pipeline rejection; stale shader output caused by missing `.hlsli` dependency tracking; and null `MTLCounterSampleBuffer` result in `plume::MetalQueryPool::queryResults()`.
- **Result:** successful. DK64 renders through `Apple iOS simulator GPU` with the complete PaperPad touch overlay. The persistent three-dot menu opens; Settings opens from it; Master Volume changed from 100% to 55% and was restored to 100%; Done returned to the live overlay. The app remained alive beyond the former crash points. A subsequent checked-in script run rebuilt, signed, installed, verified/copied the private ROM, launched PID 24243, and rendered DK64 again.
- **Artifact identity:** repeatable-script executable SHA-256 `bea17500b946bd1865374513ebb075d911350d526ae1ac0e49611bb9636072b3`; bundle ID `com.chrissotraidis.bananapad`; arm64 iOS Simulator deployment target 15.0.
- **Evidence:** `docs/VALIDATION-IOS-SIMULATOR-2026-08-31.md` and ignored PNGs under `generated/evidence/bananapad-ios-simulator/`.
- **Interpretation:** local iPadOS Simulator bring-up is no longer a blocker. Remaining work is patch-lane cleanliness, clean-candidate update rehearsal, and expanded gameplay/save/lifecycle acceptance before any physical-device claim.
- **Next step:** fold the complete working diff into external patches, stage a clean same-pin candidate, rebuild it, rehearse promotion, and roll it back recoverably.

## 2026-08-31 09:15 CDT — clean candidate, Simulator smoke, and reversible patch promotion

- **Lowest unmet goal:** G2 at entry; G3 after this step.
- **Hypothesis:** the proven BananaPad macOS/iPad integration can be expressed outside the vendor tree, replayed exactly on a fresh upstream checkout, built and run there, and promoted or reversed without losing the known-good state.
- **Step:** replace fragmented experimental patches with one cumulative BananaPad integration patch; include both upstream and BananaPad directories in the patch-series digest; stage a fresh same-pin candidate; exact-check and apply every patch; build a clean arm64 Simulator bundle; validate the generated software-sampler Metal shader; install the locked ROM; launch and visually confirm DK64/touch UI; add a 20-second launch-services smoke receipt; require that exact receipt during candidate testing; rehearse promotion without lock mutation; promote the new patch digest; exercise promotion rollback and re-promotion; archive the candidate recoverably.
- **Failure retained:** the first automated survival probe used `kill -0` through `simctl spawn`, which does not share the launched app PID namespace. The second launch-services probe combined `grep -q` with Bash `pipefail`, causing the producer's expected SIGPIPE to report failure. In both cases attached console, launch-services state, and screenshots proved the process was still live. The final probe consumes the complete launch-services table and passes.
- **Result:** successful. Candidate commit `c6730d2f244d7b2d9d8c47c94c2eecfa1bfb1a43`; recursive patched-worktree SHA-256 `47bb6c525e5b0936092e53aecda66fb8d480be4199dd38031f774e97c184f224`; patch-series SHA-256 `c5fe24f048fc3c9452ec19d2ecfef471f76a2820c213695288c332fc8a35f632`; clean Simulator executable SHA-256 `b3812118d108c8fae67c9643d0597c7dd5b3cb6dc1a887e1825a412332e79fde`. `scripts/test-upstream-update.sh` passed. Lock SHA-256 stayed `407856c6684461ec64373c4cec6f8416a2ccb8aed2d0a63337433af8bf20f938` during rehearsal, changed to `ef5078b439253ec1013b45f8a151b9664d62adc771dc9aa70f78c0f623162c35` on patch promotion, returned to the former hash during rollback, and returned exactly to the promoted hash on re-promotion.
- **Evidence:** `docs/VALIDATION-IOS-SIMULATOR-2026-08-31.md`; `generated/evidence/bananapad-ios-simulator/clean-candidate-run.png` (SHA-256 `42e60b2dd74f691e698a4da9c02a72f22e50f343fe1d48050a1a78cc1927a975`); ignored smoke receipt and promotion/rollback JSON/checkouts under `generated/validation` and `generated/upstream`.
- **Interpretation:** G2's update lane is now executable against the complete BananaPad delta, not merely the earlier compatibility patch. macOS remained the first hardened platform; iPadOS Simulator is also demonstrably runnable on this machine. No physical-device claim is made.
- **Next step:** complete G3's compressed-overlay/static-dispatch audit against the promoted patch identity, retaining the proven macOS and Simulator artifacts as regression anchors.

## 2026-08-31 10:48 CDT — G3 static execution model accepted on macOS and clean iPad Simulator candidate

- **Lowest unmet goal at entry:** G3 — Mobile-safe execution model proven.
- **Hypothesis:** the no-dynamic profile can exclude executable-mod/LiveRecomp machinery at link time while retaining the fixed AOT event table, and every game/patch/overlay/RSP/save dispatch boundary can be regenerated, inventoried, artifact-audited, and replayed from the external patch series.
- **Sequence:** native macOS remained first. The hardened macOS build exposed `event index out of bounds`; source inspection showed the no-dynamic path had skipped the fixed base-event allocation along with LiveRecomp. The profile now calls `setup_events(num_base_events())` while continuing to omit dynamic code. The rebuilt macOS app rendered DK64. Only after that repair did the iPadOS Simulator build/run proceed.
- **Step:** generate complete AOT/patch, overlay, RSP, and save manifests and their human-readable accounts; strengthen the linked-artifact audit; refresh the exact external patch; prove zero-fuzz application in isolation; build/install/launch the clean candidate; bind a 20-second Simulator smoke to source/app/ROM hashes; run the full execution-model/package audits; rehearse, apply, roll back, and reapply the promoted patch identity.
- **Result:** successful. macOS screenshot `generated/evidence/bananapad-macos/g3-hardened-run-fixed.png` SHA-256 `cd2bca0ecf89e52c18adfc24eb7804ebb19312f6af274588d9bd9533eb442cd6`; Simulator screenshot `generated/evidence/bananapad-ios-simulator/g3-hardened-smoke-pass.png` SHA-256 `beab07335ab40f9dd4577cf391c790e4deb9c042191d19caea56a40bff4cb4ce`; clean candidate executable SHA-256 `73870abcc55c5973ba7804e136097fbd5f0f8ffdfa6f0a1db708a06c277aa2b0`; recursive patched-worktree SHA-256 `721fa2509e7e280a13b9a573f5685a792e09c37f1cabe7a957139c8137ad6d56`; patch-series SHA-256 `a9be9a2d1b7a7def90b6936f1305613774cdde07899c43fbcfbe35d859bfffcb`.
- **Audit:** `audit-mobile-execution-model.sh` passed against the hardened macOS app and exact clean Simulator candidate. It accounts for 12 AOT sections, ten compressed classes, 221 generated patch functions, 158 replacements, 11 event stubs, ten registered events, 71 manual host bindings, the fixed `n_aspMain` RSP route, and the 2,048-byte `Eep16k` contract; both artifacts pass no-dynamic-code checks and the Simulator package passes its package audit.
- **Update lane:** the promoted lock SHA-256 became `e789b5e3ecf3cd1be5f55b968535daf3ab1183d0b1738541d20a857445444f6c`; rollback restored `ef5078b439253ec1013b45f8a151b9664d62adc771dc9aa70f78c0f623162c35`; re-promotion reproduced the new hash exactly.
- **Retained risks:** the shared-overlay `jal`/indirect-tail warnings, `recomp_on_new_file_start` registration discrepancy, patch compiler warnings, full audio/save route matrices, and one-off initial Simulator SIGSEGV remain recorded. They own later gameplay/correctness gates and are not misreported as executed coverage.
- **Interpretation:** G3 is met. The macOS and Simulator both run locally on this Mac; no physical iPad was attempted or required. The lowest unmet goal is G4, followed by G5's complete macOS first-play route before Simulator first-play acceptance.
- **Known-good command:** `BANANAPAD_WORKSPACE="$PWD/worktrees/dk64-upstream-candidate" BANANAPAD_SIMULATOR_APP="$PWD/generated/build/bananapad-ios-candidate/Release/BananaPad.app" BANANAPAD_SIMULATOR_BUILD_DIR="$PWD/generated/build/bananapad-ios-candidate" ./scripts/audit-mobile-execution-model.sh`.

## 2026-08-31 13:28 CDT — G5 native first-play and persistence checkpoint

- **Lowest unmet goal:** G5 — macOS first-play loop.
- **Hypothesis:** the promoted static macOS profile can sustain real menu/gameplay/map-transition/save activity, exit cleanly, and visibly reload without using Simulator or physical hardware.
- **Step:** launch the exact native bundle through accessibility-backed Computer Use; start a fresh Game 1; skip the opening sequence; control DK in Training Grounds; enter Cranky's Lab; complete Cranky's full training dialogue; observe all four training barrels spawn; enter the barrel-pickup training arena; cleanly close once; relaunch; select Game 1; and visibly reload the saved file.
- **Result:** partial success. Native Metal video, audio, keyboard input, title/Adventure menus, Training Grounds traversal, swimming, Cranky's interior transition/dialogue, four-barrel spawn cutscene, barrel entry, and timed pickup arena all executed. The first clean exit left a 2,048-byte `saves/DK64.bin` with SHA-256 `0d6b7806afbb4f023b8e71e8c5d9fc03076f651201e52007f0d63a358c86f212`; relaunch displayed Game 1 at `0%`, `000` Golden Bananas, and `001:09`, then restored DK at his Training Grounds house. G5 is not accepted: the pickup arena and remaining three barrels, DK Isles, first Golden Banana, Jungle Japes, one in-level Golden Banana, and final persisted reload remain incomplete.
- **Evidence:** `generated/artifacts/2026-08-31/g5-macos/03-reloaded-training-grounds.jpeg`; the live native run visibly reached the spawned-barrel field and pickup arena. Private save and ROM remain outside the repository.
- **Interpretation:** there is no macOS, Metal, input, save, relaunch, or iPad Simulator bring-up blocker. The current friction is the Computer Use bridge's discrete-key latency inside a timed vanilla DK64 arena; this is an automation constraint, not evidence of an app defect and not authorization to skip the route.
- **Next step:** complete the pickup arena with a bounded faster supported input route, finish the remaining training barrels without an intervening restart, then continue the exact G5 route and repeat clean save/reload acceptance.

## 2026-08-31 16:33 CDT — G5 supported held-input lane and timing diagnosis

- **Lowest unmet goal:** G5 — macOS first-play loop.
- **Hypothesis:** a default-off desktop-only key-latch override can supply the sustained A/Z and analog input that accessibility-backed Computer Use cannot hold across calls, without changing normal macOS input or any iOS/iPadOS build.
- **Step:** add bounded parsing for `BANANAPAD_TEST_KEY_TAP_FRAMES` on macOS only; retain four-frame normal input when absent or invalid; expose T/G/F/H as four-frame fine-stick aliases and U/V as sustained A/Z aliases only while a non-default override is active; rebuild, sign, launch, and exercise the exact static macOS candidate; terminate an accidentally launched non-static bundle discovered by process inspection; enter a training barrel and exercise its timed arena; cleanly relaunch the exact candidate and visibly reload Game 1.
- **Result:** the exact static candidate builds and runs. After adding sustained 1/2/3/4 stick aliases, the latest executable SHA-256 is `1c72cea998717b6a72119dcce7ad75b13d559e6b1dd104fe21951713f40982fa` and bundle SHA-256 is `3cbd263099210f168ba2c0e1d05969c1e6a53b95a1e3988f5b1e1080b3f95d41`. U produces sustained A behavior, V produces sustained Z behavior, T/G/F/H provide bounded navigation, and 1/2/3/4 provide sustained up/down/left/right while the override is active. `scripts/test-native-input-contract.sh` passes. A clean relaunch displayed the persisted Game 1 at `0%`, `000` Golden Bananas, and `003:34`, then restored DK at his house. One non-static process was found and terminated; process inspection thereafter showed exactly one static game instance and no booted Simulator. A diagnostic actor inventory after reload showed only one of the four training-barrel actors remains, at `(941.09, 732.02)`; three completions have therefore persisted.
- **Timing diagnosis:** LLDB stops the process while the VI scheduler continues to compare against wall-clock time. Detach therefore creates temporary catch-up frames; a timed-arena attempt in a debugger-contaminated process dropped from 41 to 9 during roughly five seconds of wall time and is rejected as invalid evidence. A clean process restored normal title/menu/gameplay cadence. LLDB is no longer permitted in or immediately before a timed acceptance attempt; timed evidence must include a wall-time/counter sanity check.
- **Interpretation:** the held-input seam advances native accessibility-based acceptance and remains default-off/macOS-only. It does not complete G5. The remaining work is deterministic no-debugger navigation plus completion of all four training barrels, DK Isles, the first Golden Banana, Jungle Japes, one in-level Golden Banana objective, and final save/reload.
- **Next step:** finish the training-barrel route in the clean native process, capture durable screenshots immediately, then continue the exact G5 route without substituting Simulator evidence.

## 2026-08-31 18:03 CDT — clean native orange-training completion

- **Lowest unmet goal:** G5 — macOS first-play loop.
- **Correction:** the prior claim that a chunk-local actor inventory proved three persisted training completions was invalid. Reloaded visuals showed all four barrels; actor lists vary by loaded chunk. Only observed challenge completion and the resulting overworld disappearance count as completion evidence.
- **Step:** shorten the default-off traversal latch to avoid overlapping opposite directions; add default-off macOS-only 5/6/7/8 aliases that atomically express A plus stick up/down/left/right because the accessibility bridge cannot chord two nonmodifier keys; rebuild, sign, and run the exact static bundle; use debugger-only coordinate diagnostics in discarded runs; restart without LLDB; load the persisted save; enter the orange Training Barrel; collect all five oranges before expiry; return to Training Grounds; save the resulting screenshot.
- **Result:** successful clean timed completion. The game returned to Training Grounds and the completed orange barrel disappeared from the active set. The rebuilt bundle SHA-256 is `2ee108cec6467b6b741b928aeeaae927e9e972c133b8c9d6899fd683a2c741ab`; executable SHA-256 is `e185c5b367677cd70a1dbdcabce64e7d0499b17e8253d299c40b61632c46ee21`. Deep signature verification and `scripts/test-native-input-contract.sh` pass. No Simulator or physical device was used.
- **Evidence:** `generated/artifacts/2026-08-31/g5-macos-held-input/orange-training-complete-clean.png`.
- **Interpretation:** macOS native execution and timed gameplay are working on this machine. The remaining work is game-route automation, not a macOS, Metal, Simulator, or physical-device blocker.
- **Next step:** complete the remaining three Training Grounds barrels in clean runs, obtain Simian Slam, continue through DK Isles/Jungle Japes and the required Golden Bananas, then cleanly exit and visibly reload before moving to iPadOS Simulator acceptance.

## 2026-08-31 18:48 CDT — production-duration native input and persistence correction

- **Lowest unmet goal:** G5 — macOS first-play loop.
- **Correction:** the 18:03 orange result proved a live timed challenge completion but did not persist after quitting before the full four-barrel sequence. The reloaded save displayed all four barrels. No individual Training Barrel is now claimed as durably complete.
- **Diagnosis:** the current process had initially inherited `BANANAPAD_TEST_KEY_TAP_FRAMES=30`. Direct CoreGraphics key-down/key-up events were working, but every SDL key-down also armed the 30-frame test latch, extending short corrections and distorting timed navigation. This was an automation configuration error, not a macOS, renderer, game, or Simulator limitation.
- **Step:** add and compile `scripts/hold-macos-keys.swift`, which posts named key chords for an exact wall-clock duration; cleanly quit the latched process; relaunch the exact static app with no input override; visibly reload Game 1 at `0%`, `000`, and `004:20`; traverse Training Grounds with precise production keyboard input; and enter live barrel-handling and vine-training arenas. Read-only/positioning LLDB diagnostics were not counted as clean route evidence, and no debugger was attached during a timed result claimed as acceptance.
- **Result:** the exact native app still boots, renders, accepts menu/gameplay input, reloads the save, and cleanly supports precise wall-clock holds with the default production mapping. The barrel-handling arena accepted real B interactions and three of five required throws in one timed attempt. The vine arena also launched and accepted movement/jump input. Neither attempt completed, so G5 remains open. Exactly one native process is running and no Simulator is booted.
- **Interpretation:** there is no inability to run BananaPad on this Mac or in iPadOS Simulator. The remaining macOS work is the mandated vanilla first-play progression route; the Simulator build/run path has already been proven and remains ordered after native G5-G7 acceptance.
- **Next step:** use the production-duration input path to complete all four barrels uninterrupted, obtain Simian Slam, finish the DK Isles/Jungle Japes Golden Banana route, cleanly save/exit/reload, and then repeat the accepted loop in the single iPad Simulator.

## 2026-08-31 19:26 CDT — supplied Simulator crash classified; verbatim touch UI guarded

- **Lowest unmet goal:** G5 — macOS first-play loop; this was a targeted G8 regression check requested by new crash evidence, not a reordering of formal acceptance.
- **Crash evidence:** the supplied 10:00 CDT iPad Simulator report is an `EXC_BAD_ACCESS` at `0x750` on RT64's graphics thread in `RT64::Interpreter::processDisplayLists`. Its executable UUID `3DC552E0-CCFC-3F12-B8EC-D493BE28F991` exactly matches the preserved pre-promotion Simulator executable SHA-256 `a7256823998744b728106c37650ef185af79864d764431d38e81ec018ee884b5`. It does not match the promoted candidate UUID `58C5DEC5-B257-35DC-AA5E-FA719173EDF4` / SHA-256 `47cf2d83e266422ae5980de8b1daabb7cdff9dd4f8291479448886078c18d69a`.
- **Step:** cleanly quit the sole native diagnostic process; verify no booted Simulator; boot exactly one iPad Pro 13-inch (M5) Simulator; install the exact promoted candidate and locked private ROM; run one 20-second smoke and four additional terminate/relaunch/15-second checks; inspect launch five; open the three-dot menu and settings; exercise on-screen Start through two transitions and A into Adventure file select; search for new BananaPad crash reports; terminate the app and shut down the only Simulator.
- **Result:** all five launches survived, launch five visibly rendered DK64 with the complete touch overlay, no new crash report appeared, touch Start advanced DK64 through the intro/title path, and touch A selected Adventure. The three-dot menu exposed Settings and diagnostics; the settings sheet exposed volume, Auto/1x–4x resolution, aspect ratio, touch enable/opacity, touch-layout edit/reset, diagnostics, ROM management, and Done.
- **UI fidelity:** `apple/app/ios_main.mm` is byte-for-byte identical to pinned PaperPad `74b6e45830a06c7f274c5ac1ddd7c625bc13a557`, SHA-256 `feb4e78539e4473bff324d402c158de08b9a7eb7f5038787f2dedf0038472c44`. New `scripts/test-paperpad-ui-fidelity.sh` makes that verbatim touch/menu/settings requirement executable and passes.
- **State after action:** no BananaPad process and no booted Simulator. The iPad data-container file was empty because Simulator save acceptance has not been run; no mobile progression is claimed.
- **Interpretation:** the supplied crash is real evidence for an older build, not proof that the promoted candidate currently crashes. The product-critical mobile shell is present and source-faithful, but full joystick/B/Z/L/R/C-button chords, simultaneous touch, layout editing, lifecycle, controller handoff, saves, and first-play remain mandatory functional gates.
- **Next step:** return to the exact native macOS candidate for G5, then finish the complete touch/control/menu functional matrix on one iPad Simulator, shut it down, and run the compact iPhone form-factor gate separately.

## 2026-08-31 20:28 CDT — native route mapped; touch and candidate-audit contracts strengthened

- **Lowest unmet goal:** G5 — macOS first-play loop.
- **Step:** relaunch the exact static native app with the production keyboard mapping and no booted Simulator; visibly restore Game 1; leave DK's treehouse; reach the complete four-barrel field using ordinary movement; use discarded LLDB-touched practice runs only to map barrel/world coordinates; rerun the source, package, AOT, and input audits; add a compiled tap-latch/multi-touch contract test; and make the mobile execution-model audit resolve the exact last-passing Simulator artifact from its hash-bound smoke receipt.
- **Result:** the native app again booted, rendered, restored the save, accepted gameplay movement, and reached all four live Training Barrels. No timed completion from the debugger-touched practice run is claimed. `scripts/test-paperpad-ui-fidelity.sh`, `scripts/test-touch-input-contract.sh`, `scripts/test-native-input-contract.sh`, `scripts/check-repo-safety.sh`, `scripts/audit-upstream-baseline.sh`, `scripts/check-no-dynamic-code.sh`, `scripts/audit-ios-package.sh`, and the now-default `scripts/audit-mobile-execution-model.sh` pass. The latter selects promoted candidate SHA-256 `47cf2d83e266422ae5980de8b1daabb7cdff9dd4f8291479448886078c18d69a` from the passing receipt instead of silently auditing the preserved known-crash build directory.
- **Touch contract:** the new compiled test proves independent A/B latch lengths, non-truncating tap extension, masked release, and full lifecycle release; its static half guards all N64 button masks, UIKit multi-touch roles, touch/controller merging, and analog clamping. The byte-identical PaperPad UI source remains unchanged.
- **Automation incident:** the day-old Codex Computer Use service and macOS LaunchServices stopped accepting fresh UI/input helper registrations during practice. BananaPad itself remained live and its main loop sampled normally. Infrastructure processes were restarted and every queued helper/native diagnostic instance was terminated; no affected practice action is acceptance evidence, and no game instance remains running. This is an automation-host incident, not a BananaPad defect or a reason to skip native acceptance.
- **Next step:** perform a fresh no-debugger four-barrel sequence with ordinary production input, obtain Simian Slam, and continue G5 through DK Isles/Jungle Japes, first/in-level Golden Bananas, clean exit, and visible reload. Only then boot the single iPad Simulator for the complete touch/chord/menu/lifecycle matrix.

## 2026-08-31 21:30 CDT — RT64 startup race fixed; Simulator evidence made immutable

- **Lowest unmet goal:** G5 — macOS first-play loop. This was a bounded G8 regression repair prompted by repeated ordinary-launch failure; it does not claim formal mobile acceptance ahead of G5–G7.
- **Diagnosis:** unified logs and two new crash reports proved ordinary candidate launches died by `SIGSEGV`, not watchdog or voluntary exit. Both faulted at `RT64::Interpreter::processDisplayLists + 112`, invalid address `0x750`, where release RT64 dereferenced a null `hleGBI`. A one-Simulator LLDB capture recorded first-task microcode addresses `0x741F40`/`0x760840` and exact known hashes `0x8C1C9814E75E1B4B`/`0xF8649121FAB40A06` (`F3DEX2.fifo 2.07`). The data was valid while stopped. Source ordering showed the graphics thread called `sp_complete()` before RT64 consumed the task, allowing DK64 to replace the RDRAM microcode concurrently; debugger timing masked the race.
- **Step:** keep `apple/app/ios_main.mm` byte-identical to PaperPad; move SP completion after `send_dl()` only under `BANANAPAD_NATIVE_SHELL`; add `scripts/test-renderer-task-order-contract.sh`; refresh the external integration patch; recoverably archive and freshly restage the same upstream pin; clean-build the candidate; boot exactly one iPad Simulator; run a locked-ROM 20-second non-debug smoke and four additional 12-second terminate/relaunch checks; search for new crash reports; terminate and shut down the sole Simulator; replace overwriteable candidate evidence with immutable per-run receipts; rerun exact update/package/execution-model/touch/source/safety gates.
- **Result:** candidate executable SHA-256 `a2c41ad830c096e273f55814bc49fd094662a687c3e5782ad372d4b77518609a` survived all five ordinary launches and produced no new crash report. Patch SHA-256 is `43f96da58f0f27952496f4a36ec47282e9544b6a197bdd389d7e5803c6714a65`; patch-series SHA-256 is `eecff7a84ee6c898a5c38ce32ed9561714b137f93d9cc3ab9a128854c9d29f49`; candidate worktree SHA-256 is `fb8ccbba6c9bc7c6e21a04e90549a5ece766526cc1941c3a4f3bbd6955b9063c`. `test-upstream-update.sh`, the mobile execution-model audit, no-dynamic/package checks, PaperPad UI fidelity, compiled touch contract, repository safety, and whitespace checks pass. No Simulator or game process remains.
- **Evidence model:** each successful Simulator run now writes an immutable ignored receipt under `generated/validation/ios-simulator-runs/` and separately refreshes the latest-run pointer. Candidate testing searches for its exact app/worktree/commit/ROM-bound immutable receipt, so unrelated later runs cannot erase or impersonate evidence.
- **Next step:** return to native macOS G5, complete all four Training Barrels uninterrupted with no debugger, obtain Simian Slam, finish DK Isles/Jungle Japes and Golden Banana/save-reload gates, then perform formal iPad touch/menu/control acceptance and shut it down before the separate iPhone gate.

## 2026-08-31 22:18 CDT — first barrel persisted; iPad touch/menu settings made truthful

- **Lowest unmet goal:** G5 — macOS first-play loop. The iPad work was a bounded product-surface regression pass, not a claim that G8 has superseded the ordered macOS gates.
- **Native result:** using the production macOS controls with no debugger and no Simulator, DK entered and completed the barrel-handling Training Barrel. The completed barrel disappeared from the field and the real 2,048-byte save changed to SHA-256 `eb0ef304788bf753d1a600447d0597c02f4f628c5397dc37ee8025b5e5487ec5`. The remaining three barrels were not completed and are not claimed.
- **iPad product pass:** after the native app was closed, exactly one iPad Simulator was booted. The complete PaperPad touch overlay rendered; the three-dot menu hid gameplay controls and exposed Settings/diagnostics; Settings opened; touch opacity changed to 55% and persisted over terminate/relaunch; the layout editor opened and returned through Reset/Done; touch Start advanced intro/title/menu and touch A selected Adventure and created Game 1.
- **Defect found:** the visible Auto/1x/2x/3x/4x control mapped 3x and 4x back to Auto in the older runtime contract. That violated the no-fake-settings requirement. The external ultramodern contract now carries `Resolution::Manual` plus a multiplier, BananaPad applies and invalidates it in RT64, and `scripts/test-native-settings-contract.sh` guards the route.
- **Telemetry repair:** the unchanged verbatim PaperPad settings source labels the returned dimensions as internal. The bridge had returned the drawable size instead. It now reports the effective 320x240 reference size multiplied by RT64's confirmed scale. A live relaunch showed `Renderer confirms 4.00x (1280x960 internal)` while gameplay continued behind the modal.
- **Artifact/evidence:** candidate executable SHA-256 `709a38a6675a9b422a55a36d269522cdddc010f22f2fc5a1bd2bbe6ef1792b7e`; immutable 20-second locked-ROM receipt under `generated/validation/ios-simulator-runs/`; screenshot `generated/validation/ipad-manual-resolution-20260901T0312Z/renderer-confirms-4x-1280x960.png`, SHA-256 `f30ac7646695be5b26e0cd2678bb76ed69282a8beabbc32e4c5fdfa2eb228aeb`.
- **Audit:** same-pin candidate test, mobile execution-model audit, no-dynamic/package checks, renderer-order contract, native-settings contract, PaperPad fidelity, touch contract, repository safety, and whitespace checks pass. Candidate recursive-worktree SHA-256 is `e998566a4b372e716f9880ec351a6e51eb3130ca2b9a5cae814801acb3c9a3af`; patch-series SHA-256 is `e6527b2a866a9ee2a7d38e8d16e7e1ce147bd13bdd39bd109ce071a78d62be48`.
- **State after action:** no game process and no booted Simulator.
- **Next step:** resume native macOS at the three remaining Training Barrels, obtain Simian Slam, and finish the first-play/Golden-Banana/save-reload route before completing the remaining iPad chord/controller/lifecycle/save matrix and the separate iPhone form-factor gate.

## 2026-08-31 23:04 CDT — persisted-barrel claim corrected after visible reload

- **Lowest unmet goal:** G5 — macOS first-play loop.
- **Hypothesis:** the previously observed barrel-handling completion and changed EEPROM hash meant that barrel would remain absent after a later clean reload.
- **Step:** keep every Simulator shut down; launch the exact native macOS candidate with the existing default-off, desktop-only 12-frame acceptance transport; visibly load Game 1 at `0%`, `000` Golden Bananas, and `005:32`; leave DK's treehouse; traverse to the Training Barrel field; inspect the full clearing; exercise sustained A+stick barrel-entry chords; close the native window normally; and record the resulting save identity.
- **Result:** the candidate again booted, rendered, loaded the real save, accepted sustained acceptance-only chords, and reached the field without a debugger. The fresh reload visibly showed all four Training Barrels. Therefore the earlier live barrel-handling completion did not persist across the later reload, despite the changed save hash, and the 22:18 persisted-barrel statement is retracted. The current 2,048-byte save SHA-256 is `794e297bca3767b3daa7e078d27504ac46858d81910e846ab36ea1569eef2ed9`, last written at 22:59:10 CDT. No Simulator was booted, and the native process exited normally.
- **Interpretation:** live challenge completion alone is not the durable training checkpoint. G5 must complete all four barrels and obtain Simian Slam in one uninterrupted run before quitting; only a subsequent visible reload may establish persistence. This is a correction to evidence quality, not a macOS, Metal, input, save-file, or Simulator blocker.
- **Next step:** run the uninterrupted four-barrel sequence through Simian Slam, then continue DK Isles/Jungle Japes and Golden Banana/save-reload acceptance. The already-proven PaperPad touch overlay and three-dot menu remain release-blocking, with the outstanding full chord/controller/lifecycle/save matrix scheduled for the single iPad Simulator after the native route.

## 2026-08-31 23:16 CDT — precise held-jump acceptance transport built and audited

- **Lowest unmet goal:** G5 — macOS first-play loop.
- **Problem:** the existing default-off desktop acceptance aliases could provide a four-frame precise stick tap or a long A+stick chord, but not a long jump with a small directional correction. The latter is required to automate DK64's floating-barrel collision without distorting the full jump arc. Shipping keyboard input and the PaperPad touch layer already support ordinary held input and were not changed.
- **Step:** add default-off 9/0/-/= aliases that hold N64 A for `BANANAPAD_TEST_KEY_TAP_FRAMES` while releasing up/down/left/right analog input after the normal four frames; extend the source contract and `docs/INPUT.md`; rebuild and sign the static native bundle; visibly reload the real Game 1 save; exercise sustained traversal and swimming without a debugger or Simulator; correct the route model using the documented Training Area sign behind Cranky's sign; and rerun static-input, PaperPad-fidelity, compiled touch, no-dynamic-code, signature, and whitespace checks.
- **Result:** the rebuilt macOS executable SHA-256 is `f9d6c0ef611b0ffa62165850789f7eacff587a7f9b806fee21add08b10d948f1`; the bundle build SHA-256 is `aed214e8e1e20cc639d37e7d934e4bc96a8ef65996893756d1689cc0fc313851`. The native input contract, byte-identical PaperPad UI check, compiled touch contract, no-dynamic-code audit, deep signature verification, and whitespace check pass. No Simulator was booted. A discarded navigation run entered the pool beside the exit tunnel instead of the Training Area; it is route practice only and claims no progression. The native process exited normally. The current 2,048-byte save SHA-256 is `c87a24255fe746e7e0adcde16e192793c0c58f8918c96ce6e4bdbc1bf0b2f536`, last written at 23:09:17 CDT.
- **Interpretation:** the remaining barrel-entry automation gap now has a bounded, macOS-only transport that cannot alter production or mobile input when the environment override is absent. G5 remains incomplete until the uninterrupted four-barrel/Simian-Slam checkpoint and subsequent Golden Banana/save-reload route are visibly proven.
- **Next step:** relaunch from DK's house, take the Training Area path rather than the exit-tunnel pool, validate one precise alias against a floating barrel, and then complete all four challenges without quitting before returning to Cranky for Simian Slam.

## 2026-09-01 00:54 CDT — exact iPad/iPhone touch shell proven; stale save isolated

- **Active goal:** G8/G9/G10 Apple touch product lane after the user accepted G5 native feasibility and directed work away from exhaustive desktop routing.
- **Step:** preserve the byte-identical pinned PaperPad `ios_main.mm`; connect BananaPad's SDL controller scan to `PaperPad_SetPhysicalControllerConnected`; extend the touch contract; build exact arm64 Simulator candidate `8dc234dcea3e2e9ea11d16bfdfa419d7a0a613b640b6aeea863fbd6a1a635936`; audit package/no-dynamic-code; boot only one iPad; diagnose its initial map-load exit; recoverably back up the stale experimental save folder; rerun unchanged with clean disposable state; exercise the full overlay, touch Start/A, three-dot menu, and Settings; shut the iPad down; boot only one iPhone; repeat locked-ROM smoke, compact layout/menu/Settings, and touch Start; shut it down; update README/input/status/validation records.
- **Result:** the current exact executable passed immutable locked-ROM 20-second smoke on both targets. iPad touch Start skipped the intro and entered the main menu, then touch A selected Adventure and reached an empty file slot. iPhone compact touch Start skipped the intro. Both displayed the complete PaperPad N64 overlay, persistent three-dot menu, and native Settings. The iPad receipt is `generated/validation/ios-simulator-runs/8dc234dcea3e2e9ea11d16bfdfa419d7a0a613b640b6aeea863fbd6a1a635936-dda170d764b6145b0bb6281fab013755c75d7585ec456287203e90d5175e6482-20260901T055244Z-pid79285.json`; the iPhone receipt ends `20260901T054922Z-pid78347.json`. Product-source SHA-256 is `17562f563d4fb2e9b916351597ba8a2759e1a15871953eb51a53c0965c511965`. No Simulator or game process remains.
- **Save incident:** the first iPad smoke inherited a stale 2,048-byte save folder from older experimental/crashing builds and faulted by `SIGBUS` in `func_global_asm_80650E20` during map-geometry setup. Backing up only that disposable save and launching the unchanged binary made the fault disappear and produced the passing smoke. This is now an explicit save-corruption/migration investigation, not a renderer, boot, or touch blocker.
- **Evidence:** `docs/VALIDATION-APPLE-TOUCH-2026-09-01.md`; ignored screenshots under `generated/validation/ipad-touch-20260901T0047CDT/` and `generated/validation/iphone-touch-20260901T0049CDT/`; exact hashes recorded in the validation document.
- **Tests:** PaperPad UI fidelity, compiled touch input, native settings/input, renderer ordering, update evidence, repository safety, iOS package, and no-dynamic-code checks pass for the current source/candidate boundary.
- **Next step:** finish Simulator-safe lifecycle/layout/menu-suppression checks and retain physical multi-finger/controller items for G12; then complete G8/G9 save/reload first-play and resume G6 full-game breadth.

## 2026-09-01 01:03 CDT — compact layout, resume, orientation, and current save relaunch pass

- **Active goal:** remaining Simulator-safe G10 shell behavior on iPhone.
- **Step:** boot only the iPhone 17 Pro Simulator; relaunch the same installed exact candidate with its current app-created save; use a console-attached run to distinguish one transient voluntary launch exit from a crash; open Settings and the touch layout editor; return through Reset/Done; send the app Home and foreground it from the BananaPad icon; rotate through both supported landscape orientations; inspect the complete overlay after every boundary; attempt Simulator Sleep/Wake without claiming it when no visible suspension occurred; terminate and shut down the sole Simulator.
- **Result:** the candidate remained live with its current 2,048-byte save SHA-256 `6bac2800d45107e251842b177ba1f86878073056c964e404530de3c455a51758`. The editor opened and reset/closed, Home→foreground resume restored the live overlay, and both landscape orientations retained every control inside the safe area while DK64 rendered. The earlier one-second process exit was voluntary and did not recur in the evidence run; no crash report was produced. Sleep/wake remains untested because the Simulator control did not visibly suspend the app.
- **Evidence:** `generated/validation/iphone-lifecycle-20260901T0100CDT/opposite-landscape.png`, SHA-256 `377a22f2113613e89205a1643137c47628db9e71130072e196fec40d6fcf28eb`; validation detail in `docs/VALIDATION-APPLE-TOUCH-2026-09-01.md`.
- **State after action:** no game process and no booted Simulator.
- **Next step:** finish menu/ROM-flow suppression and iPad lifecycle checks, then complete game-visible G8/G9 save/reload first-play. Physical multi-touch/controller acceptance remains G12.

## 2026-09-01 01:25 CDT — startup variant repaired; exact touch candidate replayed on both form factors

- **Active goal:** G8/G9 first-play and remaining Simulator-safe G10 after the usable PaperPad touch/menu slice.
- **Diagnosis:** an ordinary iPad relaunch reproduced the startup/map-load failure after the earlier SP-order repair, disproving the tentative stale-save explanation. RT64 logged text address `0x00741F40`, alternate text hash `0x8EDC2B2BC4D1E3B6`, data address `0x00760840`, data hash `0xF8649121FAB40A06`, and embedded identity `RSP Gfx ucode F3DEX fifo 2.07`. The data hash matched canonical F3DEX2 2.07, but RT64 recognized only canonical text hash `0x8C1C9814E75E1B4B`.
- **Step:** add the exact alternate hash to RT64's F3DEX2 2.07 table only under `BANANAPAD_NATIVE_SHELL`; compile that definition into RT64; guard the mapping, array size, and compile boundary; refresh the external integration patch; recoverably archive the stale staged candidate; stage a clean same-pin candidate; clean-build it; boot only the iPad, run an exact locked-ROM smoke and inspect its full overlay/three-dot menu; shut it down; boot only the iPhone, run the same artifact and inspect its compact overlay; shut it down; then run the upstream candidate, package, no-dynamic-code, UI, input, renderer, update-evidence, safety, and whitespace gates.
- **Result:** working executable `606cc86d18007d9b6aef9dd732ae51537dc8187d7a0bdc04af8984f98eb3a636` passed one 20-second smoke plus four further ordinary iPad launches with the previously suspect state. Clean replayed executable `77d18de47a9e5ac6fb0239d6703aba5f8048a34826c12f60c2271e272b0bb3fd` passed immutable 20-second smokes on both iPad and iPhone; no new crash report or rejected-ucode log appeared. iPad receipt ends `20260901T061907Z-pid91638.json`; iPhone receipt ends `20260901T062130Z-pid92762.json`. The exact iPad candidate visibly opened the PaperPad three-dot menu; the exact iPhone candidate visibly displayed every compact touch control. No Simulator remains booted.
- **Replay identities:** integration patch `6560adbb4a8b3c8a239f5353329d788c615631fc03f366c7980b0aeacc7dbd5e`; patch series `501c7555885fe4997eb62173cf95ecd7787633cb7fb3486d06a12334c063eb16`; recursive candidate worktree `a7d09b22646780518091b3b60c72679d3ea3c5c8c7b95773a4a5115bc0c74155`; product source `17562f563d4fb2e9b916351597ba8a2759e1a15871953eb51a53c0965c511965`. `scripts/test-upstream-update.sh` passes.
- **Visual evidence:** `generated/validation/ipad-staged-candidate-20260901T0120CDT/three-dot-menu.png`, SHA-256 `85ebc32ec7ea7ed9a740d6d031c5774c745050359a69e917669649b01a5ca542`; `generated/validation/iphone-staged-candidate-20260901T0122CDT/touch-overlay.png`, SHA-256 `a680a8010d1a33a10554753087d156603753dae810f7dfe64cf2f147a99fddcf`.
- **Interpretation:** touch/menu implementation and the thin upstream-replay lane are working; the prior save-corruption hypothesis is retracted. This does not claim full G8/G9 first-play, physical multi-touch chords/controller handoff, physical-device acceptance, or release authorization.
- **Next step:** on one Simulator at a time, complete iPad game-visible first-play/save-reload plus lifecycle/ROM-flow rows, shut it down, repeat the compact iPhone loop, then return to G6/G7 breadth. Physical chords and controller handoff remain G12.

## 2026-09-01 01:46 CDT — exact candidate reaches touch gameplay and visibly reloads saves on iPad and iPhone

- **Active goal:** G8/G9 progression-loop acceptance after exact G10 touch-shell and startup repair.
- **Step:** boot only iPad; install and launch clean replayed executable `77d18de47a9e5ac6fb0239d6703aba5f8048a34826c12f60c2271e272b0bb3fd`; use only on-screen Start/A to create Game 1; allow the mandatory new-file sequence to reach DK's treehouse; exercise virtual-stick movement, A jump, and B attack; hash the real 2,048-byte primary/backup save; terminate and relaunch; use touch to reopen Adventure/Game 1 and inspect its counters; capture evidence; terminate and shut iPad down; then repeat the entire bounded route on the compact iPhone layout before shutting it down.
- **iPad result:** touch created Game 1, reached playable treehouse, moved DK, jumped, and attacked. Primary save SHA-256 `6d16991320b7786abc8e8fe3f0f4964052a71d24ca48aef256062523be159fe1`; relaunch visibly restored `0% / 000 / 00:04`. Evidence `generated/validation/ipad-first-play-20260901T0136CDT/reloaded-game1.png`, SHA-256 `fda4bcc73a05aef9ae67e8cd1150560db3905526bad4ea0baa7f27d080507e71`.
- **iPhone result:** compact touch independently created Game 1, reached playable treehouse, moved DK, jumped, and attacked. Primary save SHA-256 `54a04cb7dc031627db84910b72feca2205d75854d138943698874c15e2d2e558`; relaunch visibly restored `0% / 000 / 00:04`. Evidence `generated/validation/iphone-first-play-20260901T0145CDT/reloaded-game1.png`, SHA-256 `da49980d376d74a78c02a245186cac78105727cb878e197f80cfd463d18251e7`.
- **State after action:** both game processes terminated and no Simulator booted. No physical-device claim is made.
- **Interpretation:** the final artifact now has real game-visible touch stick/A/B and save/reload proof on both form factors. G8/G9 remain open because PRD D4/D8 additionally require Training Grounds, DK Isles, first Golden Banana, a Jungle Japes objective, persisted progression/reload, and broader controls/lifecycle evidence.
- **Next step:** resume only the iPad save and complete the required progression route with touch, folding in Z/L/R/C-direction and lifecycle/ROM-flow checks; shut it down before repeating on iPhone.

## 2026-09-01 02:07 CDT — remaining iPad controls proven; touch route reaches Training Grounds

- **Active goal:** G8 progression-loop acceptance and remaining Simulator-safe G10 input/lifecycle rows.
- **Step:** verify the exact staged artifact/worktree/patch identities and clean process state; boot only iPad; resume the existing Game 1; exercise Z, L, R, and all C directions against live DK64 behavior; press B immediately before Home and foreground the app; then use only touch A/stick to leave the treehouse, jump down, cross to the cave, traverse its bend, and enter the Training Grounds clearing; terminate and shut down the sole Simulator after the route entered the pool.
- **Control result:** Z visibly crouched; C-left/right rotated the original camera; C-up/down changed camera distance; R recentered it; and Home→foreground restored DK idle instead of replaying the pending B action. L is delivered and contract-covered but has no distinct visible effect in this early DK state. Physical simultaneous chords and controller handoff remain unclaimed.
- **Progression result:** touch navigation reached the Training Grounds clearing and then the pool. No Training Barrel, Simian Slam, DK Isles, Golden Banana, Jungle Japes objective, or persisted progression checkpoint is claimed. Pool screenshot `generated/validation/ipad-controls-training-20260901T0205CDT/training-pool.png`, SHA-256 `7389bb5913872e0e9acaba4dbce5c3a1aa7c409be0dccf6e63a43cf4928de547`.
- **State after action:** app terminated; iPad shut down; no Simulator or game process remains.
- **Interpretation:** baseline touch gameplay controls are now substantially proven on iPad, and the previously confused route into Training Grounds is mapped. G8 remains open on the four-barrel/Simian-Slam and later first-play progression requirements.
- **Next step:** repeat the mapped treehouse→cave→clearing route, remain outside the pool, complete all four Training Barrels uninterrupted, obtain Simian Slam, and verify a changed save before proceeding to DK Isles.

## 2026-09-01 02:18 CDT — Training Grounds route reproduced; swimming behavior classified

- **Active goal:** G8 four-barrel/Simian-Slam progression checkpoint.
- **Step:** resolve Computer Use's real drag parameter shape (`from_x/from_y/to_x/to_y`), resume the exact staged iPad Game 1 with no other Simulator booted, reproduce the touch-only treehouse→cave→Training-Grounds route, and exercise surface swimming along the pool bank before terminating and shutting down the sole Simulator.
- **Result:** the correct drag API is now available for genuine virtual-stick gestures, though its action duration is fixed and short. The route again reached the Training Grounds/pool. B produced the forward swim stroke and stick direction steered DK. The approached bank is a high collision edge rather than the lower climb-out, so the attempt did not reach a barrel. No save/progression change is claimed.
- **State after action:** no game process and no booted Simulator. Source/candidate identities remain unchanged.
- **Interpretation:** the repeated route and responsive swimming rule out boot, map-transition, touch-stick, A/B, or pool-input failure. The remaining immediate task is spatial: take the pool's lower exit or avoid entering it, then complete the barrels.
- **Next step:** resume the mapped route, follow the bank to the low exit with B+stick, capture the barrel clearing before entering a challenge, then complete all four barrels uninterrupted and verify the save change.

## 2026-09-01 02:37 CDT — mobile acceptance reoriented to the touch product

- **Active goal:** G10 Apple-shell closure and G11 candidate qualification after user acceptance of the proven boot/play/save loop.
- **Correction:** repeating Training Barrels, Golden Bananas, and Jungle Japes independently on each Simulator was conflating full-game G6/G7 qualification with the G8/G9 form-factor platform gates. The latest route attempt also omitted the required Cranky dialogue before barrel spawn; that is a game-route mistake, not a touch or platform failure.
- **Step:** use the sole iPad Simulator to reconfirm touch traversal from the persisted exact-candidate save; stop before spending another cycle on tutorial navigation; shut the Simulator down; revise the goal loop so G8/G9 accept verified ROM → gameplay input → save → terminate/relaunch → visible reload; preserve full progression under G6/G7 and physical multi-touch/controller work under G12; rerun the product contract, safety, package, execution-model, and same-pin upstream-update gates.
- **Result:** G8 and G9 are accepted as Simulator platform gates. G10's Simulator implementation is accepted with physical multi-finger and real-controller ownership explicitly deferred to G12. `test-paperpad-ui-fidelity.sh`, `test-touch-input-contract.sh`, `test-native-settings-contract.sh`, `test-native-input-contract.sh`, `test-renderer-task-order-contract.sh`, `test-update-evidence-contract.sh`, `check-repo-safety.sh`, `audit-mobile-execution-model.sh`, and `test-upstream-update.sh` all pass against executable `77d18de47a9e5ac6fb0239d6703aba5f8048a34826c12f60c2271e272b0bb3fd` and product source `17562f563d4fb2e9b916351597ba8a2759e1a15871953eb51a53c0965c511965`. The tested patch-set identity was promoted into `dependencies.lock.json`; rollback restored the previous lock and re-promotion reproduced patch-series SHA-256 `501c7555885fe4997eb62173cf95ecd7787633cb7fb3486d06a12334c063eb16`. After recording the latest observed upstream `main`, lock SHA-256 is `fa410d15d5667501c727d141a1a83dec840c49c4cdaf46ccffd3badcb7b1c986`.
- **State after action:** no booted Simulator and no BananaPad/DK64Recompiled game process. No Training Barrel, Simian Slam, Golden Banana, or later-game progression is claimed.
- **Upstream observation:** `check-upstream.sh` reconfirmed stable `1.0.1` and found `main` advanced to `1b22409b5297dbb710843cc4493d9b7a4a303bdc`. The two post-observation commits improve D-pad descriptions and add a one-instruction `us.toml` patch. They are categorized and recorded, but not promoted from an unreleased `main` without a named BananaPad defect.
- **Next step:** finish the G11 exact-candidate/clean-clone documentation and prepare the G12 physical-device checklist; return to G6/G7 breadth as qualification work, not as a prerequisite for a usable touch shell.

## 2026-09-01 03:09 CDT — original icon packaged; current candidate re-smoked on both touch targets

- **Active goal:** finish G11 exact-candidate qualification after accepting the touch product loop.
- **Step:** replace the unproven legacy paper/controller icon with original BananaPad artwork; preserve the prior asset for audit history; record the generation prompt, source/output hashes, processing, exclusions, and physical appearance follow-up; archive the pre-icon candidate recoverably; stage and clean-build the same promoted upstream/patch identities with the new product source; then boot and test only one Simulator at a time, iPad first and iPhone second.
- **Result:** source master SHA-256 `9ef7f7fa2a0e2da390bd67192a9c8e7e94979346e38202f4b76a24bf424bf4d8`; packaged 1024×1024 asset SHA-256 `e8c1213d036e12f1acd6b388c537ddb72d9dded5a3e7bb52f53731e63410a882`. Product-source SHA-256 is `35024db329d0690338f1cf05c1a2c9c2592f86c419c18ec3756d7f07f3a8bf16`; clean executable SHA-256 is `f4fab84e2b0b99c8cba45ccfd576f256810eebd660f25fbf00805564b910f182`. It passed locked-ROM 20-second smokes on iPad and iPhone and visibly rendered the full/compact touch overlays with the persistent three-dot menu. Immutable receipts end `20260901T080731Z-pid36872.json` and `20260901T080854Z-pid37598.json`. After recoverably promoting this app into the canonical candidate path, its first launch reproduced the known intermittent `func_global_asm_80650E20` `SIGBUS`; the immediate identical-executable retry passed and produced receipt `20260901T081349Z-pid39633.json`. The consolidated candidate, update, icon, package, no-dynamic-code, and execution-model gates then passed.
- **Evidence:** `generated/validation/screenshots/icon-candidate-ipad-smoke.png`, SHA-256 `c4afd3969e79607e48b57010d6e3f2a1b530342eaace82da914436eee003ab93`; `generated/validation/screenshots/icon-candidate-iphone-smoke.png`, SHA-256 `7da623573167f171e25963bf6eb51026f1d86ea7472ac62793ca5d11e8614eab`. The preceding gameplay executable remains the source of full interactive stick/button/save/reload evidence because the only product-source change was icon/provenance packaging.
- **State after action:** both apps terminated; both Simulators shut down. Stable upstream remains `1.0.1`; observed `main` remains recorded but unpromoted. Physical icon appearances remain G12.
- **Next step:** rerun the consolidated exact-candidate gates and close all G11 work possible before repository review/commit enables a true clean-checkout reproduction.

## 2026-09-01 03:48 CDT — upstream candidates now build from their own generated inputs

- **Active goal:** close G11's updateability gap without reopening the already-accepted boot/play/touch loop.
- **Problem found:** candidate staging isolated the DK64Recompiled checkout, but decompression, game/RSP generation, patch generation, and parts of the Apple build still defaulted to promoted global inputs. A future upstream candidate could therefore compile stale generated code and appear greener than it was.
- **Step:** add candidate-aware input/output overrides; add deterministic candidate preparation and build entry points; move the patch stamp out of the source checkout; extend Simulator receipts to bind generated game manifest, patch manifest, and decompressed-ROM hashes; require those identities in the upstream test; rebuild from a freshly staged same-pin candidate; smoke only the existing sole iPad Simulator; then terminate the app and shut it down.
- **Result:** candidate generation repeated deterministically. Game manifest `ae6c0ed0deb141a8ee15b3eeb62d73099558ea5b4d8f12fbd906661df62615c6`, patch manifest `2fdbd3ea3029ea6a28975b524d51a4a2302042fa1e7f3ea4c04d8347db0b7c35`, and decompressed-ROM SHA-256 `04bca239f17380aa2a97e8f04715a09f1f05b74df9c057b609558408fd39c91a` are candidate-local and receipt-bound. The clean checkout remained `a7d09b22646780518091b3b60c72679d3ea3c5c8c7b95773a4a5115bc0c74155`; executable `f4fab84e2b0b99c8cba45ccfd576f256810eebd660f25fbf00805564b910f182` remained byte-identical; receipt schema 4 smoke passed; `test-upstream-update.sh` passed.
- **State after action:** no booted Simulator. Product source is `f00692789f60448dc665611a71d43477cdfbd209517c3c5558a156126e15c7b3`. G11 remains open only where an actual committed clean checkout or broader qualification evidence is required; the touch product itself is not blocked.

## 2026-09-01 04:01 CDT — one-command bootstrap and new-pin qualification path completed

- **Active goal:** make first use and future DK64Recompiled updates operationally simple without weakening the evidence bar.
- **Gap found:** local setup still exposed a long diagnostic command list instead of the PRD's one-command entry point. Separately, a genuinely newer candidate could reach `needs-full-validation` but had no supported way to bind completed gameplay/save/settings review back into the promotion gate.
- **Step:** add `bootstrap-bananapad.sh` with prepare/macos/ios/all endpoints and an immutable identity receipt; make reruns safely reuse an already-verified normalized ROM; add `qualify-upstream-candidate.sh`; require its exact commit/worktree/product/generated-input/app/Simulator identities plus explicit patch, deterministic-generation, macOS gameplay, iPad touch gameplay, save/reload, settings, and audit checks; consume that record in the upstream gate; archive/invalidate it on rollback or regeneration; document both workflows.
- **Result:** the real `--target prepare` command passed, then the real `--target all` command passed. Receipt `generated/validation/bootstrap-last-run.json` binds promoted commit `c6730d2f244d7b2d9d8c47c94c2eecfa1bfb1a43`, product `f00692789f60448dc665611a71d43477cdfbd209517c3c5558a156126e15c7b3`, ROM SHA-1 `cf806ff2603640a748fca5026ded28802f1f4a50`, game manifest `ae6c0ed0deb141a8ee15b3eeb62d73099558ea5b4d8f12fbd906661df62615c6`, patch manifest `2fdbd3ea3029ea6a28975b524d51a4a2302042fa1e7f3ea4c04d8347db0b7c35`, hardened macOS executable `15ab44367c0f1b6b911032e41e69f1b474a384620d23c15bd9d97e685b424661`, and build-only Simulator executable `af270e364e1b88f25e26c0187b097879803b00c2be34e9c52da0e4a333e35f3f`. Both package/no-dynamic-code checks passed.
- **State after action:** bootstrap did not boot or launch any Simulator; none is booted. The previously accepted candidate executable and interactive touch evidence remain authoritative for gameplay. G11 still needs a literal fresh checkout, which first requires the intentionally uncommitted project to be reviewed and committed.

## 2026-09-01 04:08 CDT — clean snapshot exposed and removed a hidden SDL2 checkout dependency

- **Active goal:** prove G11 setup from a clean, isolated Git source snapshot without committing or changing the user's root worktree.
- **Step:** export only project-owned files into ignored `generated/validation/clean-snapshot-20260901T0905Z`, initialize and commit them as a clean local Git snapshot, then run the one-command bootstrap using public network sources and the external private ROM.
- **Finding:** DK64Recompiled and its recursive graph, PaperPad, SunPad, and the host tools downloaded and verified, but the build correctly stopped because `ref/paperpad/ref/SDL2` was absent. The existing root had this ignored checkout from PaperPad's own clone workflow, so BananaPad had been relying on unexplained local state.
- **Fix:** promote PaperPad's exact SDL 2.32.10 source revision `5d249570393f7a37e037abf22cd6012a4cc56a71` into BananaPad's dependency lock, clone it explicitly into PaperPad's expected ignored path, disable its push URL, verify it on every source check, and document why it is a first-class input. Dependency-lock SHA-256 is now `0dd7c242d09f2c377ca7d47da255a65388cd8fbd5fac3add0c7bff4cc3eba3c3`.
- **Interpretation:** the clean exercise found a real reproducibility defect rather than merely copying current caches. A second clean snapshot must now prove the corrected full path before G11's fresh-directory build can be called green.

## 2026-09-01 04:27 CDT — isolated clean Git snapshot reproduction passes

- **Active goal:** finish G11's clean-source/update mechanics while preserving the root's intentionally uncommitted state.
- **Step:** create clean snapshot `generated/validation/clean-snapshot-20260901T0910Z`, commit only project-owned files, run bootstrap from public pins plus the external private ROM, fix the first-use patch-stamp directory ordering exposed by the clean run, and resume entirely within that isolated snapshot.
- **Result:** clean snapshot commit `2ad0ffb4c8595b359f1681b142832c9f9c50c434` has an empty Git status. Its bootstrap receipt SHA-256 is `b110ce48dc97bac46f37a2e58c0177fe4ddb464aacecf40b3019655a473baeff`. It independently reproduced game manifest `ae6c0ed0deb141a8ee15b3eeb62d73099558ea5b4d8f12fbd906661df62615c6` and patch manifest `2fdbd3ea3029ea6a28975b524d51a4a2302042fa1e7f3ea4c04d8347db0b7c35`, then built and audited hardened macOS executable `e3e852c0dd149c7ae37c9f0a3008db970a8348125fb92a276bccb69eb3fff30d` and Simulator executable `164ae8b9ecbfcffc4da8592eca206ee63b08243bdd77bbeb083b8b98b9e3cd42`. No dynamic code/package violation was found.
- **State after action:** no Simulator was booted. The root working tree was neither committed nor cleaned. The clean-source/update mechanics portion of G11 is green; remaining G11 rows are the still-open G6/G7 correctness/progression matrix, not touch-shell or setup work.
## 2026-09-01 04:45 CDT — Goal corrected to the Apple product boundary

- **User correction:** DK64Recompiled is the accepted working game. Stop spending BananaPad time replaying progression; finish the iOS/iPadOS touch UI, three-dot menu, and mobile shell.
- **Action:** rewrote the goal loop/PRD/status/readiness matrix so full-game replay is inherited from upstream and reopened only for a named BananaPad/upstream-update regression. Stopped the macOS app and restored the original 2,048-byte save (`d7d12c3ea50ea935046962cf8464198efeb7da3664b7dbc388a09810c647bb21`).
- **Mobile change:** ROM replacement remains validate-before-write and atomic. Remove ROM now requires a second destructive confirmation, explicitly preserves saves/settings, and remains limited to `DK64.z64` plus `rom.cfg`. Added `test-rom-management-contract.sh` to the product gate.
- **Direct iPad evidence:** executable `ed39411f2505854dae4c9d595b7b636fa966f7a1a4389b97111b6c82dc0d0368`, product source `a430cee8f335831c2e1a78e12d8b8483a131bb7ec67581236fb86cefa8f39150`. The one active iPad Simulator showed the full overlay, persistent three-dot menu, Settings, Game ROM actions, and the new preservation confirmation. Removal was cancelled; no data was deleted. Private receipt: `generated/validation/mobile-ui-last-run.json`.
- **State after action:** no game process and no booted Simulator. Next Apple-owned gap is native-UI/controller suppression and controller reconnect behavior, then lifecycle/audio interruption.

## 2026-09-01 04:52 CDT — exact rebuilt iPad shell accepted; native UI blocks controller input

- **Active goal:** finish the iOS/iPadOS Apple shell; do not replay upstream DK64 progression.
- **Change:** added a UIKit scene monitor without modifying PaperPad's byte-identical `ios_main.mm`. The game adapter now returns neutral buttons/sticks while the application is inactive or any native view controller is presented, covering Settings, ROM dialogs, diagnostics/share UI, and background transitions. Both controller buttons and right analog are suppressed. Added `test-native-ui-suppression-contract.sh` to the product gate and added the monitor to the replayable upstream integration patch.
- **Build/result:** after adding neutral rearm, iOS Simulator executable `739fecd9aed836bf235c15ddfac2f80b50babd88fe4850e410e4d79524505117`; product source `8ac6fcbe1e781ac650b80c8175bcbb89da4ce3d637a0c277ce595d14c96f43e9`; current working patch series `9e990184425119b1065b779f082819dcee91a39ad56d6d66d65f821cdcf76aa0`. Compiled symbols confirm the monitor and suppression bridge are linked. PaperPad fidelity, touch, native input/settings, ROM management, suppression/rearm, readiness, and whitespace checks pass.
- **Direct iPad UI evidence:** with exactly one iPad Simulator booted, the rebuilt app visibly exposed the full N64 touch overlay, persistent three-dot menu, Settings, touch controls/opacity/layout actions, diagnostics, and Manage Game ROM. Evidence is under `generated/evidence/g10/mobile-ui-20260901/`; `mobile-ui-last-run.json` binds the exact executable/product. This was UI-only acceptance, not another gameplay route. Physical-controller behavior is not falsely claimed and remains G12 hands-on evidence.
- **State after action:** the app was terminated and the iPad Simulator shut down; no Simulator is booted. The next Apple-owned implementation gap is lifecycle/memory/audio interruption handling, followed by sustained-operation and packaging/device evidence. The current working patch should be replayed/promoted once the remaining mobile-source changes settle rather than rebuilding the isolated candidate after every small Apple-shell edit.
- **Lifecycle substrate audit:** the pinned SDL2 build already registers all UIKit active/background/foreground/terminate/memory-warning notifications, emits the SDL low-memory event, pauses its AudioQueue for `AVAudioSession` interruptions, restarts it on interruption end, and retries on foreground for omitted end notifications. `test-mobile-lifecycle-contract.sh` now makes those dependencies explicit in the product gate. Runtime audible interruption and pressure behavior remain honest Simulator/physical acceptance items rather than missing source implementation.
- **Bounded lifecycle run:** the exact app had already launched twice for UI acceptance. A later lifecycle-only session encountered the recorded intermittent startup exit on the initial launch and its one permitted identical retry, before any lifecycle action was taken. Per the timebox rule, the Simulator was shut down and no third retry was made. This does not invalidate the earlier exact-artifact overlay/menu acceptance and does not reopen upstream gameplay; repeated lifecycle observation remains the next named Apple-shell acceptance item after the startup defect is isolated or avoided by a fresh candidate build.

## 2026-09-01 05:25 CDT — real iPhoneOS build and package audit pass

- **Active goal:** prove that the accepted touch/menu shell is a real iOS/iPadOS build, without replaying DK64 or confusing Simulator evidence with hardware acceptance.
- **Step:** add a deterministic device build entry point; configure Xcode for `iphoneos`, arm64, iOS 15, both iPhone/iPad families, the native Apple shell, and the AOT/no-dynamic-code profile; disable signing so the build is independent of a developer account; build; inspect the Mach-O platform and bundle metadata; run the package audit; bind the exact result in `generated/validation/ios-device-last-build.json`.
- **Result:** the real unsigned device app built successfully at `generated/build/bananapad-ios-device/Release/BananaPad.app`. Executable SHA-256 is `2519ee937149d35d367a0b823ac1831a37f710492c9fdcf3658119de5675c2f5`; `LC_BUILD_VERSION` is iPhoneOS with minimum 15.0; `UIDeviceFamily` contains iPhone and iPad; the static/no-dynamic-code and iOS package audits pass. No ROM is packaged. Product source is `8ac6fcbe1e781ac650b80c8175bcbb89da4ce3d637a0c277ce595d14c96f43e9`; working patch series is `9e990184425119b1065b779f082819dcee91a39ad56d6d66d65f821cdcf76aa0`.
- **Honesty boundary:** this is compile/package acceptance, not a signed install or physical-device runtime claim. Physical touch chords, controller handoff, audio routes/interruption, sustained operation, thermal/memory behavior, and icon appearance remain G12.
- **State after action:** no game process and no booted Simulator. Next work remains lifecycle/sustained-operation acceptance and preparing an exact signed-device handoff; upstream gameplay replay stays out of scope unless BananaPad introduces a named regression.
- **Signed-device handoff:** `build-bananapad-ios-device.sh` now optionally enables automatic Apple development signing when `BANANAPAD_DEVELOPMENT_TEAM` is supplied. `install-bananapad-ios-device.sh` requires an explicit device identifier, rejects an invalid signature, re-audits the exact app, installs with `devicectl`, and can launch it. The contract passes. This Mac currently reports zero valid signing identities and no attached device, so no physical install is claimed.
- **Bounded fresh-Simulator lifecycle attempt:** with every other Simulator shut down, created one disposable iPad Pro 13-inch Simulator, waited for its terminal boot state, installed exact executable `739fecd9aed836bf235c15ddfac2f80b50babd88fe4850e410e4d79524505117`, and copied the verified private ROM into only that app container. CoreSimulator stalled subsequent `simctl launch` requests without producing a BananaPad process, console output, or new crash report. This is distinct from the earlier two `func_global_asm_80650E20` reports and yielded no lifecycle evidence. The exact hung `simctl` helpers were terminated, the sole disposable Simulator and its derived ROM copy were deleted, and the original/generated ROM inputs remain unchanged. No Simulator or game process remains. Do not repeat the unchanged launch request until the local CoreSimulator launch service is reset or a known-good device state is used.
- **CoreSimulator recovery check:** while no Simulator was booted, restarted the idle Simulator app and CoreSimulator service, then created a second fresh disposable iPad, waited through its full data migration to terminal boot, installed the same exact app, and independently verified the copied ROM SHA-1. Both CLI and Home Screen launch again stalled before any BananaPad process existed; the Home Screen's system and app icons also remained placeholder-grey. No new BananaPad report or app log was created. This confirms a local iOS 26.5 Simulator launch-service/runtime failure for this bounded session, not evidence that BananaPad entered and crashed. The second disposable device and its derived ROM copy were deleted; no Simulator or game process remains.
- **Known-device confirmation and data correction:** booted only the previously successful iPad Simulator to distinguish a new-device migration fault. An intended recoverable container backup was attempted before reinstall, but `get_app_container` rejected the shut-down device and the compound diagnostic command continued after boot; the ensuing clean reinstall therefore removed the old disposable iPad app container without a backup. That removed its live 2,048-byte test-save copy and app settings, not the repository/generated ROM inputs, macOS save, iPhone Simulator evidence, or recorded iPad save hash/screenshots. This sequencing error is recorded rather than concealed. The clean app received a verified ROM, but the known device again showed unresolved placeholder icons for Apple system apps and BananaPad, and launch never created a BananaPad process. UI evidence `generated/evidence/g10/core-simulator-launch-failure-20260901/known-device-placeholder-icons.png` has SHA-256 `7fd8b86a8d751590977495a5ec67a02896ca7e72d13098afc9a412b6c4e052b4`. It was shut down; no Simulator or game process remains.

## 2026-09-01 06:16 CDT — repeated iPad lifecycle, native Settings, and both landscapes pass

- **Correction:** the grey placeholder icons seen immediately after resetting CoreSimulator were transient while LaunchServices rebuilt its system-app database, not proof of a corrupt runtime. After the known iPad completed that work, bounded `simctl launch` returned PID 77150 and the exact current app rendered normally. The earlier runtime-failure classification is retracted; the retained placeholder screenshot/log now document the transient diagnosis path only.
- **Sequence:** with exactly one iPad Simulator booted, launch exact executable `739fecd9aed836bf235c15ddfac2f80b50babd88fe4850e410e4d79524505117`; open the persistent three-dot menu; Home/background; foreground; confirm the menu remains; dismiss and confirm the complete overlay returns; perform two further Home→foreground cycles and observe rendering resume; render both landscape orientations; open native Settings; background/foreground with Settings retained; tap Done and confirm overlay restoration. No game input or progression route was exercised.
- **Result:** all three lifecycle cycles passed. The app process remained alive, menu/Settings state remained coherent, renderer output resumed, touch controls returned only after native UI dismissal, and both iPad landscapes fit the overlay. Receipt `generated/validation/ipad-lifecycle-last-run.json` binds the executable/product and screenshot hashes. Evidence is under `generated/evidence/g10/ipad-lifecycle-20260901/`.
- **Limits:** Simulator exposes no memory-warning menu in this Xcode version; no memory-pressure claim is made. Audible AVAudioSession interruption/routes, real-controller handoff, physical multi-finger chords, thermal behavior, and physical icon presentation remain device gates. The app was terminated and the sole Simulator shut down.
- **Terminate/relaunch follow-up:** booted the same sole iPad once more and ran three bounded clean launch→10-second initialization survival→terminate cycles. PIDs 80321, 80367, and 80421 each remained the exact BananaPad process through the observation window; no report newer than the pre-existing 05:04 `func_global_asm_80650E20` incident appeared. The app was terminated after each cycle and the Simulator shut down. This closes repeated terminate/relaunch initialization evidence without replaying gameplay.

## 2026-09-01 07:38 CDT — mobile goal enforced; update lane and device build completed without gameplay

- **User correction and active goal:** DK64Recompiled works. Stop launching or playing it to reconfirm that fact; finish the iOS/iPadOS touch UI, persistent three-dot menu, mobile builds, README, and maintainable upstream-update lane.
- **Goal/document correction:** `PRD.md` and `GOAL-LOOP.md` now make PaperPad authoritative for the verbatim touch/menu source and SunPad secondary. Same-pin maintenance explicitly uses non-game mobile-shell evidence; gameplay reopens only for a named BananaPad or newer-upstream compatibility risk.
- **Harness correction:** removed the experimental no-ROM `--shell-smoke`. Native ROM setup intentionally waits for import, causing `simctl launch` to wait indefinitely; this was a harness error, not a product failure. The sole iPad Simulator and stuck launcher were terminated, and no Simulator remains booted.
- **Upstream result:** corrected the malformed integration-patch hunk count, restaged `1.0.1`, regenerated exact candidate game/patch/RSP inputs, and clean-built Simulator executable `b9c1d2a90d5dd47ec945413e642a827ca5b9d0836e403de29c75cfd242517955`. Exact PaperPad UI (`feb4e78539e4473bff324d402c158de08b9a7eb7f5038787f2dedf0038472c44`), touch/menu, ROM-management, renderer, package, no-dynamic-code, patch replay, and archived baseline contracts passed. Product source is `d7e4873026331e89acbee9e30d91f5daf5b5de8023b011650f040783040fc0cb`; patch series is `8f9e051ed2d643a39a4618acd0c31f3072317732bd29ade1a8687256e91b9505`.
- **Reversibility result:** same-pin rehearsal, promotion, actual rollback, and re-promotion passed. Promoted dependency-lock SHA-256 `c6b661c21b3a7698972197dfe41f149b6089cecbc95126f89d74469628aecd0d` was reproduced exactly after rollback.
- **Device-build defect/fix:** a fresh default unsigned build found `native_build_args[@]: unbound variable` under the system Bash strict mode. Replaced the empty-array expansion with explicit signed/unsigned build branches and guarded it in `test-ios-device-build-contract.sh`. The rerun built the universal iPhone/iPad `iphoneos` app, passed package/no-dynamic-code audits, and produced executable `2beed7aea437cff7f213662ea6ceceb1e76b24ee59c4b41478c9cd6e2f8ad6d4`.
- **Private IPA handoff:** added and tested `package-unsigned-ipa.sh`. It rejects signed input, audits the source app and resulting standard `Payload/BananaPad.app` archive, and produced ignored ROM-free IPA `generated/packages/BananaPad-unsigned-2beed7aea437cff7f213662ea6ceceb1e76b24ee59c4b41478c9cd6e2f8ad6d4.ipa`, SHA-256 `709b8b46d57177df1fb3745fe45005d5222ab283a2ae7801dfb2a83d31a9460e`. This is a private artifact, not publication authorization.
- **State and next step:** no Simulator or game process is running. The remaining honest gate is G12 on signed physical iPad/iPhone hardware: multi-finger chords, real-controller handoff, audible routes/interruptions, thermal/memory soak, and icon presentation. No physical device or signing identity is available on this Mac.

## 2026-09-01 08:03 CDT — first-use and update workflows completed without gameplay

- **Active goal:** finish the iOS/iPadOS product boundary and handoff; DK64Recompiled remains the accepted game implementation, so no gameplay route was launched.
- **Guided updates:** added `manage-upstream.sh` as the normal check/evaluate/status/promote/rollback interface over the isolated candidate lane. `evaluate-latest` safely reports no work when the promoted stable tag is current; a newer pin remains stopped at the exact affected-route qualification gate rather than being promoted merely because it builds.
- **One-command setup:** expanded `bootstrap-bananapad.sh` to `prepare`, `macos`, `simulator`/`ios`, `device`, and `all`. `device` and `all` now build the universal unsigned iPhone/iPad app and create or reuse an audited, immutable, ROM-free private IPA. Bootstrap never boots or launches a Simulator.
- **Direct result:** a real `--target all` run passed from the verified private ROM through deterministic generation, hardened macOS/Simulator/device builds, no-dynamic-code/package audits, and IPA packaging. Receipt `generated/validation/bootstrap-last-run.json` binds product source `d7e4873026331e89acbee9e30d91f5daf5b5de8023b011650f040783040fc0cb`, macOS executable `5bd373999d475253d5e8a7ec3388029de09a1e4a5b650a31435b2794f7a3b21a`, Simulator executable `739fecd9aed836bf235c15ddfac2f80b50babd88fe4850e410e4d79524505117`, device executable `2519ee937149d35d367a0b823ac1831a37f710492c9fdcf3658119de5675c2f5`, and IPA `8730180e8921aa25fd92b85c25444961f78ab617d8ff8ee5ee4e7d9cd6de5e9f`.
- **Receipt hardening:** added `audit-bootstrap-receipt.sh`; bootstrap now independently verifies its receipt against the promoted lock, current product/generated identities, artifact bytes, and device/IPA package audits before reporting success.
- **Honesty boundary:** no simulated device is booted and no game process is running. The remaining G12 work requires signing plus physical iPhone/iPad hardware for touch chords, real-controller handoff, audible routes/interruption, sustained thermal/memory behavior, and icon presentation.

## 2026-09-01 08:25 CDT — update manager survives real newer upstream drift

- **Goal:** prove that the maintainable update lane handles an actual upstream change, rather than relying only on a same-pin rehearsal, without relaunching or playing DK64.
- **Candidate:** exact upstream `main` commit `1b22409b5297dbb710843cc4493d9b7a4a303bdc`, isolated label `upstream-main-20260901`. The promoted tag/commit remained `1.0.1` / `c6730d2f244d7b2d9d8c47c94c2eecfa1bfb1a43`.
- **Result:** the external BananaPad patches applied cleanly; candidate-only ROM/game/RSP/patch inputs regenerated deterministically; game manifest changed to `a03d80b5be4cfe5c9beab8e380a73dd3ba0f34ab12af12244d82c96c32803846`; the macOS support core and iOS/iPadOS Simulator app built; candidate executable `5e1ccc954965e95f6da6ffe85bff9ade31df631003d0f2affc9079235688f6cc` passed no-dynamic-code and package audits. No Simulator was booted.
- **Promotion guard:** the manager returned `needs-full-validation` for the genuinely newer untagged runtime and did not promote it. This is the intended safe stop, not a build failure.
- **Rollback proof:** `rollback-candidate` archived the candidate under `generated/upstream/rollbacks/20260901T132438Z`. The dependency lock remained `c6b661c21b3a7698972197dfe41f149b6089cecbc95126f89d74469628aecd0d`, the normal Simulator executable remained `739fecd9aed836bf235c15ddfac2f80b50babd88fe4850e410e4d79524505117`, the normal device executable remained `2519ee937149d35d367a0b823ac1831a37f710492c9fdcf3658119de5675c2f5`, and the pristine promoted checkout remained at `c6730d2f244d7b2d9d8c47c94c2eecfa1bfb1a43`.
- **Next boundary:** wait for a newer stable tag or deliberately select a fix for a named regression; do not follow `main` unattended. Physical signed-device G12 remains the only unavailable product acceptance gate.

## 2026-09-01 08:55 CDT — rejected-candidate rollback hardened; clean mobile candidate restored

- **Release-gate finding:** after rejecting newer `main`, release readiness correctly failed because rollback had archived the candidate checkout and metadata but left its generated inputs and macOS/iOS build trees at the canonical candidate paths.
- **Fix:** `rollback-upstream-update.sh` now archives all five candidate components together. `test-update-evidence-contract.sh` requires the checkout, metadata, generated inputs, macOS build, and iOS build to move into the recovery directory. The rejected real-main outputs are complete under `generated/upstream/rollbacks/20260901T132438Z`.
- **Restore/result:** a fresh isolated same-pin evaluation rebuilt the Apple support core and iOS/iPadOS Simulator app without booting a Simulator or launching DK64. It reproduced executable SHA-256 `b9c1d2a90d5dd47ec945413e642a827ca5b9d0836e403de29c75cfd242517955`; exact PaperPad UI, touch, persistent three-dot menu, ROM management, renderer ordering, package/no-dynamic-code, deterministic generation, patch replay, repository safety, update evidence, and release readiness all pass.
- **Goal state:** all locally executable mobile-shell, documentation, packaging, bootstrap, and upstream-maintenance work is green. No Simulator or game process is running. G12 remains the active external gate because this Mac has no Apple signing identity or attached physical iPhone/iPad.

## 2026-09-01 09:04 CDT — physical-device acceptance handoff made artifact-bound

- **Remaining-gap audit:** `security find-identity -v -p codesigning` still reports zero valid signing identities; `devicectl` still reports no physical devices; no Simulator is booted. G12 therefore cannot be observed on this Mac yet.
- **Local completion:** added `preflight-bananapad-device-acceptance.sh`. It requires an explicit device, rejects an invalid signature, re-audits the ROM-free app, resolves the physical device through `devicectl`, optionally installs/launches the exact app, and records signed-bundle, executable, source, lock, patch, product, and device identities in an ignored private receipt.
- **Hands-on boundary:** every preflight creates a per-device `ACCEPTANCE.md` covering icon/first launch, all N64 controls, every required Z chord, the persistent three-dot menu, settings/layout/ROM flows, controller takeover/disconnect/reconnect, lifecycle, audio routes/interruption, save/reload hashes, 30-minute form-factor operation, and the 90-minute release soak. Its decision is always pending until a human records observed results.
- **Verification:** the actual current unsigned device app is rejected before device resolution. A controlled signed/connected-device mock produced a complete schema-valid pending receipt and worksheet; the new contract and device-build contract pass. This tests the handoff mechanics only and makes no physical-device claim.

## 2026-09-01 09:35 CDT — first-class private README and final tracked handoff

- **User direction:** the accepted Simulator game/touch result is sufficient for this handoff; make the README first class like PaperPad, commit the complete BananaPad project, and merge it to private `main`.
- **README:** added the project-owned app icon, PaperPad-style requirements and one-command onboarding, exact supported-ROM boundary, first-launch flow, user-facing touch/settings behavior, capability and supported-game tables, diagnostics guidance, artifact-bound physical-device command, and FAQs. The text keeps physical-device and publication claims explicitly separate from the proven Simulator product.
- **Verification:** every local README link resolves; the pinned PaperPad `ios_main.mm` remains byte-identical at SHA-256 `feb4e78539e4473bff324d402c158de08b9a7eb7f5038787f2dedf0038472c44`; repository safety and the complete non-game product-candidate validation pass. Candidate executable remains `b9c1d2a90d5dd47ec945413e642a827ca5b9d0836e403de29c75cfd242517955`, product source `d7e4873026331e89acbee9e30d91f5daf5b5de8023b011650f040783040fc0cb`, patch series `8f9e051ed2d643a39a4618acd0c31f3072317732bd29ade1a8687256e91b9505`, and dependency lock `c6b661c21b3a7698972197dfe41f149b6089cecbc95126f89d74469628aecd0d`.
- **State:** no Simulator or game process is running. The GitHub repository is private; this handoff does not authorize public source or binary distribution.
